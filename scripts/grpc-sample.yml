# gRPC Load Testing Example
#
# ⚠️  SECURITY WARNING: This example has TLS disabled (insecure: true)
# This should ONLY be used for local development/testing.
# For production or Internet-facing services, set:
#   tls: true
#   insecure: false

protocol: grpc
target: localhost:50051

# Connection and load settings
concurrency: 20
rate: 100
duration: 2m

# gRPC-specific configuration
grpc:
  service: "helloworld.Greeter"
  method: "SayHello"
  message: '{"name": "LoadTest"}'
  timeout: 30s
  tls: true  # ✅ SECURE DEFAULT - Use tls: false ONLY for local dev/testing
  insecure: false  # ✅ SECURE DEFAULT - Set to true ONLY for local dev/testing
  # For local development/testing, you may use the following INSECURE settings:
  # tls: false  # ⚠️  LOCAL DEV ONLY - disables encryption
  # insecure: true  # ⚠️  UNSAFE - disables certificate verification
  metadata:
    authorization: "Bearer token123"
    x-request-id: "loadtest-{{worker_id}}"

# Optional: Add authentication
# auth:
#   type: oauth2_client_credentials
#   token_url: https://auth.example.com/token
#   client_id: grpc-client
#   client_secret: secret123
